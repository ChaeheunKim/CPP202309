#include <iostream>

using namespace std;

int main() {
	int one = 1; //one 이라는 변수에 1 정수 저장
	int two = 2; //two 라는 변수에 2 정수 저장
	int sum = one + two;  // sum이라는 변수에 one과 two 더한 값 저장

	cout << "1+2= " << sum << endl;  //출력

	return 0;
}